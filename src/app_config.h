#ifndef APP_CONFIG_H
#define APP_CONFIG_H

#include "raylib.h"

struct Config {
    Color backgroundColor;
    int fontSize;
    float baseFontSize;
    int baseScreenWidth;
    int baseScreenHeight;
};

#endif